import { DemandsListView } from '@/views/demands-list-view';

/**
 * Route — the expo-router entry for `/ocorrencias`, the list screen.
 */
export default function OcorrenciasIndexRoute() {
  return <DemandsListView />;
}
