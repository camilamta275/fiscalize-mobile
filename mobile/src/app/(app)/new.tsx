import { DemandFormView } from '@/views/demand-form-view';

/**
 * Route — the expo-router entry for `/ocorrencias/new`, pushed on top of the
 * list by its FAB.
 */
export default function NewOcorrenciaRoute() {
  return <DemandFormView />;
}
